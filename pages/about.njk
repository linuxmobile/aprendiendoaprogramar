---
title: Sobre mi
layout: page
customClass: page-about
icon: "/img_src/avatar_memoji.webp"
permalink: /about/
---

{# Intro #}
<header class="wave-border wave-border-index">
<div class="container ex-normal d-flex d-flex-wrap">
<div class="intro">

{% markdown %}
Hola! Soy __linux__, Un futuro __Fullstack Developer__ de Nucba. En este lugar van a poder encontrar mis notas sobre lo que me gusta escribir __linux__, __tecnología__, __programación__.

En este sitio, vas a poder encontrarte todas las notas de cosas que he aprendido a lo largo de este último tiempo. ¡Espero que te sirvan de ayuda!
{% endmarkdown %}
</div>

<div class="coordinate">

{% markdown %}
- <span>From</span> Argentina
- <span>Location</span> Tres Arroyos, Buenos Aires
- <span>Github</span> github.com/linuxmobile
- <span>LinkedIn</span> linkedin.com/in/braiandiez
- <span>Email</span> bdiez19@gmail.com
{% endmarkdown %}

{% if settings.look4job %}
  <div class="job">
    <span>Available for a full-time job!</span>
  </div>
{% endif %}
</div>

<p class="contact">
<a class="button blue no-effect" href="mailto:bdiez19@gmail.com">
  {% if settings.look4job %}Contrátame{% else %}Contacto{% endif %}
</a>
<a class="button black" href="{{ '/' | url }}">🖊 Mis notas</a>
<a class="button black" href="/animes/">📚 Lista de anime</a>
</p>

</div>
</header>

{# Skills #}
<div class="container wide d-flex d-flex-wrap">
  {% for group in skills %}
    <div id="{{ group.name | lower }}" class="section skills">
      <div class="div-h2">
        <h2>{{ group.name }}</h2>
      </div>
      <div class="list-of">
        {% for item in group.list %}
          {% set itemInfo = item | getTech(techs) %}
          {% if itemInfo.url %}
            <a href="{{ itemInfo.url }}" target="_blank">
              <div class="badge black hover-translate">
                <img class="keep-original" src="/img/about/{{ itemInfo.icon }}" alt="{{ itemInfo.name }}" height="20" width="20">
                <span>{{ itemInfo.name }}</span>
              </div>
            </a>
          {% else %}
            <div class="badge black hover-translate">
              <img class="keep-original" src="/img/about/{{ itemInfo.icon }}" alt="{{ itemInfo.name }}" height="20" width="20">
              <span>{{ itemInfo.name }}</span>
            </div>
          {% endif %}
        {% endfor %}
      </div>
    </div>
  {% endfor %}
</div>

{# Education & Experiences #}
<div class="container d-flex d-flex-wrap wide">
  {% for sec in cv %}
    <div id="{{ sec.name | lower }}" class="section exp-edu">
      <div class="div-h2">
        <h2 class="d-flex align-items-center">{{ sec.name }}</h2>
      </div>
      <div class="list-of">
        {% for item in sec.list %}
          <div class="item">
            <div class="d-flex d-flex-wrap align-items-center">
              <div class="where">
                {% if item.logo %}
                <div class="logo{% if item.bgwhite %} bg-white{% endif %}">
                  <img class="keep-original" src="/img/about/exp/{{ item.logo }}" alt="{{ item.where }}'s logo">
                </div>
                {% endif %}
                <div class="place">
                  {% if item.url %}
                    <a href="{{ item.url }}" target="_blank" rel="noopener noreferrer">{{ item.where }}</a>
                  {% else %}
                    {{ item.where }}
                  {% endif %}
                </div>
                <div class="date">{{ item.date }}</div>
              </div>
              <div class="title">
                <h4>{{ item.title }}</h4>
                <div class="where-2">
                  <span>
                    {% if item.url %}
                      <a href="{{ item.url }}" target="_blank" rel="noopener noreferrer">{{ item.where }}</a>
                    {% else %}{{ item.where }}
                    {% endif %}
                  </span>,
                  <span class="date-2">{{ item.date }}</span>
                </div>
                {% for p in item.activity %}
                  <p>{{ p | safe }}</p>
                {% endfor %}
                {% if item.tech %}
                  <div class="tech">
                    {% for item2 in item.tech %}
                      {% set itemInfo = item2 | getTech(techs) %}
                      {% if itemInfo.url %}
                        <a href="{{ itemInfo.url }}" target="_blank">
                          <div title="{{ itemInfo.name }}" class="skill-small hover-translate">
                            <img class="keep-original" src="/img/about/{{ itemInfo.icon }}" alt="{{ itemInfo.name }}" height="20" width="20">
                          </div>
                        </a>
                      {% else %}
                        <div title="{{ itemInfo.name }}" class="skill-small hover-translate">
                          <img class="keep-original" src="/img/about/{{ itemInfo.icon }}" alt="{{ itemInfo.name }}" height="20" width="20">
                        </div>
                      {% endif %}
                    {% endfor %}
                  </div>
                {% endif %}
              </div>
            </div>
          </div>
        {% endfor %}
      </div>
    </div>
  {% endfor %}
</div>

{# Projects #}
<div class="container wide">
  <div id="projects" class="section projects">
    <div class="div-h2">
      <h2 class="d-flex align-items-center">
        Proyectos
      </h2>
    </div>
    <div class="list-of-all d-flex d-flex-wrap">
      {% for item in projects %}
        <div class="list-wrapper">
          <div class="list-of">
            <div class="title">
              <a href="{% if item.source %}{{ item.source }}{% else %}{{ item.url }}{% endif %}" target="_blank">
                <h4>{{ item.icon }}<span>{{ item.title }}</span></h4>
              </a>
              <p>{{ item.description | safe }}</p>
              {% if item.tech or item.tech_text %}
                <div class="tech">
                  {% if item.tech %}
                    {% for item2 in item.tech %}
                      {% set itemInfo = item2 | getTech(techs) %}
                      {% if itemInfo.url %}
                        <a href="{{ itemInfo.url }}" target="_blank">
                          <div title="{{ itemInfo.name }}" class="skill-small hover-translate">
                            <img class="keep-original" src="/img/about/{{ itemInfo.icon }}" alt="{{ itemInfo.name }}" height="20" width="20">
                          </div>
                        </a>
                      {% else %}
                        <div title="{{ itemInfo.name }}" class="skill-small hover-translate">
                          <img class="keep-original" src="/img/about/{{ itemInfo.icon }}" alt="{{ itemInfo.name }}" height="20" width="20">
                        </div>
                      {% endif %}
                    {% endfor %}
                  {% endif %}
                  {% if item.tech_text %}
                    {% for item3 in item.tech_text %}
                      <span class="tech-text">{{ item3 }}</span>
                    {% endfor %}
                  {% endif %}
                </div>
              {% endif %}
            </div>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</div>
